<!DOCTYPE html>
<html>
  <head>
    <title>Mafom</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="header">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <%= link_to "MaFom", events_path, class: "navbar-brand p-3"%>

        <ul class="float-left nav justify-content-end ">
          <li class="nav-item">
            <% if user_signed_in? %>
              <span><%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link active mx-2 btn btn-primary" %></span>
              <li><%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link active mx-2 btn btn-primary" %></li>
            <% else %>
              <%= link_to "Log in",  user_session_path, class: "nav-link active mx-2 btn btn-primary" %>
              
            <% end %>          
          </li>
          <li class="nav-item">
            <% unless user_signed_in? %>
              <%= link_to "Sign_up", new_user_registration_path, class: "nav-link btn btn-outline-primary"%>
             <% end %>
          </li> 
        </ul>
      </nav>
  </div>
    <%= yield %>
  </body>
</html>
  